spring.application.name=peshop.back-end
#  Usu�rio do banco de dados

spring.datasource.username = root

#  Senha do banco de dados

spring.datasource.password = Yuri@37719582

# URL de conex�o com o MySQL (host, porta e nome do banco)

spring.datasource.url = jdbc:mysql://localhost:3306/petshop

# Dialeto do Hibernate para o MySQL 8

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Como o Hibernate deve lidar com as tabelas (create, update, none, validate, etc)

spring.jpa.hibernate.ddl-auto = update

# Exibir os comandos SQL no console

spring.jpa.show-sql = true

# Formatador para deixar o SQL gerado mais leg�vel no console

spring.jpa.properties.hibernate.format_sql = true